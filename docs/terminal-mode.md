# Terminal Mode

Terminal Mode is intended for administrators who prefer text output or need to run Atlhas1x without opening an HTML report automatically.

```powershell
python atlhas1x.py --terminal --report intermediate
```

You can also open `Atlhas1x_Terminal.bat` from Windows Explorer.

The terminal output is a summary. Use an Advanced HTML report when complete technical inventories and normalized report sections are needed.
