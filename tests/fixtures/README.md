# Benign test fixtures

This directory is intentionally free of malware samples. Tests create temporary
text or executable-named files containing only Atlhas1x marker strings and use
the `TEST ONLY` local YARA rules in `rules/local/`.
